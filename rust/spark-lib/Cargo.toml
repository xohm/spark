[package]
name = "spark-lib"
version = "0.1.0"
rust-version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
ahash.workspace = true
anyhow.workspace = true
glam.workspace = true
half.workspace = true
ordered-float.workspace = true
miniz_oxide.workspace = true
serde.workspace = true
smallvec.workspace = true
itertools.workspace = true
serde_json.workspace = true
zip.workspace = true
image.workspace = true
hnsw.workspace = true
rand_pcg.workspace = true
space.workspace = true
zstd.workspace = true

# ruzstd is dev-only now (codec comparison in examples/codec_bench.rs). libzstd (the
# `zstd` crate, above) is the runtime zstd decoder: ~3.8x faster than ruzstd in real
# wasm and, unlike zrip, decodes correctly on wasm32. See bench/REPORT.md.
[dev-dependencies]
ruzstd.workspace = true
